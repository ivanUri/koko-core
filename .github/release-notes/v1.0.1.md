## Koko v1.0.1

- CreepJS maths parity via V8 native `Math.*` callbacks
- Chrome-style profile layout (`browser/templates/`, user-data-dir sessions)
- Profile bundles and versioned template catalog (`browser/catalog/`)
- Homebrew bottle includes `browser/catalog/` snapshots

```bash
brew tap ivanUri/tap && brew upgrade koko
```